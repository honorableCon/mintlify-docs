---
title: 'Introduction'
description: 'Référence API Kiriku (focus Billing)'
---

## Welcome

Cette section documente l’API Kiriku via une spécification OpenAPI. La version actuelle couvre en priorité le module Billing (crédits, factures, abonnements internes) et les endpoints admin associés.

<Card
  title="Spécification OpenAPI"
  icon="file-code"
  href="/api-reference/openapi.json"
>
  Consulter le fichier OpenAPI utilisé par Mintlify
</Card>

## Authentication

Les endpoints sont authentifiés via un Bearer token (JWT). Les endpoints admin nécessitent en plus un rôle `admin`.

```json
"security": [
  {
    "bearerAuth": []
  }
]
```
